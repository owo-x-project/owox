# Web Specs

## 役割

旧 Owox WebUI 仕様の入口。ADR-0003 採用後は `owox` shell / plugin UI host / plugin 固有 UI の仕様へ再編する。

## 参照

- `SPEC-interaction-workbench-review.md`: 旧 Workbench MVP、Repo Editor、Session Monitor、検収導線。`owox` 本体 UI と `owlcore` plugin UI へ分割対象
