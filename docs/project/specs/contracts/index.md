# Contracts Specs

## 役割

旧 OpenAPI と JSON Schema による外部契約仕様の入口。ADR-0003 採用後は plugin API、plugin manifest、repo layout schema、`owlcore` schema へ再編する。

## 参照

- `SPEC-api-v0-contracts.md`: 旧 v0 HTTP API。serverless / plugin API first 方針により再評価対象
- `SPEC-json-schema-v0.md`: 旧 v0 JSON Schema。`owlcore` `.owox/owlcore/` schema へ再編対象
