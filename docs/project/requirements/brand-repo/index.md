# brand-repo Requirements v2

## 役割

`brand-repo` v2 は brand repo / brand context の将来拡張要求です。v0 `owox` と v1 `owlcore` の基礎が成立した後に扱います。

## 管轄

- owner: `brand-repo`
- version: `v2`
- responsibility: ブランド固有 context と project repo 作業への接続方針

## 参照

- `REQ-brand-repo-context.md`: brand repo と brand context の将来拡張
