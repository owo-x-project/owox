# Phase 02 owlcore Plugin

## 目的

`owlcore` v1 の serverless repo-native 公式 plugin を実装できる粒度へ仕様化し、CLI / library / plugin UI を作る。

## 完了条件

- `.owox/owlcore/` layout が固定される。
- `owlcore` CLI command set が固定される。
- Context / Work Contract / Evidence / Verifier / Event Log の schema が固定される。
- `owox` plugin UI として project context / evidence / verifier 状態を表示できる。

## Task

- `task-001-owlcore-layout-schema.md`
- `task-002-owlcore-cli.md`
- `task-003-owlcore-core-store.md`
- `task-004-owlcore-plugin-ui.md`
